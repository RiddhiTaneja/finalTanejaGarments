<!DOCTYPE html>
<html>
<head>
  <title>All Products</title>
</head>
<body>
  {{#if category}}
    <h1>All {{capitalize category}}</h1>
  {{else}}
    <h1>All Products</h1>
  {{/if}}
  <ul>
    {{#each products}}
      <li>{{name}} - {{price}}</li>
    {{/each}}
  </ul>
</body>
</html>
